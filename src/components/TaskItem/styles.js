const styles = () => ({
    cardActions: {
        display: "flex",
        alignItems: "center",
        justifyContent: "flex-end"
    },
    card: {
        marginTop: 15
    }
});

export default styles;